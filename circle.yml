# Configure java version for CircleCI
machine:
    java:
        version: oraclejdk8
# Configure final artifact location
general:
    artifacts:
        - "target/SQMovecraft.jar"
# Tests
test:
  override:
    - mvn clean install -B
